// Actions
// -------------------

const initialState = {
  userAgent: null,
  isBot: null,
};

// Reducer
// -------------------
export default function reducer(state = initialState, action) {

  switch (action.type) {

    default:
      return state;
  }
}

// Actions (action creators)
// -------------------
